h1 Epoch vs Score

= render 'filter'

- @charts.each do |algo_params, chart|
  div.chart[data-series=convert_chart_data(chart)
    data-chart-title="#{algo_params.to_a.map { |k, v| "#{k}=#{v}" }.sort.join(' ')}"
    data-chart-x-name=@x_name]

  table.table.table-striped.table-hover
    thead
      tr
        th=@x_name
        th Score
    tbody
      - chart.reverse.each do |x, score|
        tr
          td = x
          td = score
